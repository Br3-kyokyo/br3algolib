#include <bits/stdc++.h>
using namespace std;
#define REP(i, n) for (int i = 0; i < n; i++)
int i, j, k;

int med(int a, int b, int c) {
    if (a > b) {
        if (c > a) return c;
        else return a;
    } else {
        if (c > b) return c;
        else return b;
    }
}

vector<>

vector<string> insertion_sort(vector<int> vector_int) {
    for (int i = 1; i < vector_int.size(); i++) 
      if (vector_int[i] < vector_int[i - 1]);
}

int main() {}

// testcase
//
// 5
// aaaaaaaaaa
// aaaaaaaaaa
// aaaaaaaaaa
// bbbbbbbbbb
// bbbbbbbbbb